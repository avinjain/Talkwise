# OpenAI
OPENAI_API_KEY=sk-your-openai-api-key-here
GPT_MODEL=gpt-4o

# NextAuth (generate a real secret: openssl rand -base64 32)
NEXTAUTH_SECRET=change-this-to-a-random-secret
NEXTAUTH_URL=https://talkwise.life

# Database (Railway: set to /data for persistent volume)
DB_DIR=/data
